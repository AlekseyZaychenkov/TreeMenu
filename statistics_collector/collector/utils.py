from collector.models import Platform


def get_existing_platforms():
    # platforms = Platform.objects.all()
    names = ["All"]
    # for platform in platforms:
    #     names.append(platform.name)

    return names
